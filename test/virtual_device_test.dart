import 'package:test/test.dart';

import 'package:virtual_device/virtual_device.dart';

void main() {
  group('IosSimulator', () {
    test('.discoverAll', () {
      // IosSimulator.discoverAll();
    });
  });
}
